{{#if session.isAuthenticated}}
  <a {{action 'logout'}} class="btn btn-danger btn-sm">Logout</a>
{{else}}
  {{#link-to 'login' class="btn btn-success btn-sm"}}Login{{/link-to}}
  {{#link-to 'sign-up' class="btn btn-success btn-sm"}}Sign up{{/link-to}}
{{/if}}
